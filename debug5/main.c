#include <stdio.h>

int main() {
    float distance = 12.345;
    printf("Distance: %f km\n", distance);
    return 0;
}

/**
REASONING:
1. the data type used is wrong, it should be float because it doesn't need a high precision like scientific measurement
2. the format specifier is wrong it should be %f

*/
