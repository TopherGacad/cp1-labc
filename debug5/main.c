#include <stdio.h>

int main() {
    double distance = 12.345;
    printf("Distance: %.3f km\n", distance);
    return 0;
}

/**
REASONING:
The format specifier is incorrect, instead of d, use f in double
also use decimal specifier to have exact numbers of decimals

*/
