#include <stdio.h>

int main() {
    float distance = 12.345;
    printf("Distance: %.3f km\n", distance);
    return 0;
}

/**
REASONING:
    i. Used the float data type since the statement only represents 3 decimals
    ii. Changed it into correct format specifier

*/
