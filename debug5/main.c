#include <stdio.h>

int main() {
    double distance = 12.345;
    printf("Distance: %.3lf km\n", distance);
    return 0;
}

/**
REASONING:
1. Changing the %d into %f indicating that it is with decimal places
2. added the printf ("Distance: %.3lf km\n", distance);

*/
