#include <stdio.h>

int main() {
    char name[] = "Juan";
    printf("Name: %s\n", name);
    return 0;
}

/**
REASONING:
    1. "string" should be "char[]", because string isn't a command availabe to C language.

*/
