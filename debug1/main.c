#include <stdio.h>

int main() {
    char name[50] = "Juan";
    printf("Name: %s\n", name);
    return 0;
}

/**
REASONING:
1. the data type used is wrong, it should be char because in c language there's not built in string type
2. we use [50] or the maximum characters it can hold up

*/
