#include <stdio.h>

int main() {
    char name [] = "Juan";
    printf("Name: %s\n", name);
    return 0;
}

/**
REASONING:
The code is not valid because the data type used is string instead of char. Also I added this symbol[]
    to define the array.

*/
