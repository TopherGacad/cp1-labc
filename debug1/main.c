#include <stdio.h>

int main() {
    char name[] = "Juan";
    printf("Name: %s\n", name);
    return 0;
}

/**
REASONING:
         The code was not able to run because string is not a valid data type in C. works on c# though
         To store text, C requires the use of a character array (char name[]).

*/
