#include <stdio.h>

int main() {
    char name[] = "Juan";
    printf("Name: %s\n", name);
    return 0;
}

/**
REASONING:
    The code uses "string". C does not have String type to create string variables.
    Instead, to make a string in C, we use a character array (char[]).

*/
