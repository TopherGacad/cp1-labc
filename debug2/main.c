#include <stdio.h>

int main () {
    int age = 20;
    double gpa = 1.75;

    printf("Age: %d, GPA: %lf\n", age, gpa);

    return 0;
}

/**
REASONING:
// Changed the format specifier for gpa from %d to %lf (or %f)

*/
