#include <stdio.h>

int main() {
    int age = 20;
    float gpa = 1.75;

    printf("Age: %d, GPA: %.2f\n", age, gpa);

    return 0;
}

/**
REASONING:
    i. You should declare the correct data type if it has decimals.
    ii. Must remove the 1 in format specifier
    iii. You should input the age first before gpa


*/

