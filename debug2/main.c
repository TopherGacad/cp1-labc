#include <stdio.h>

int main() {
    int age = 20;
    double gpa = 1.75;

    printf("Age: %d, GPA: %.2f\n", age, gpa);

    return 0;
}

/**
REASONING:
i. Need to remove the 1 from format specifier.
ii. Input the word 'age' first before the word 'gpa'.

*/
