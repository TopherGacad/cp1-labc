#include <stdio.h>

int main() {
    char grade [] = "A";
    printf("Grade: %s\n", grade);
    return 0;
}

/**
REASONING:
1. the char variable for strings should be declared
as char (variable_name)[] = "string_value";

*/
