#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
1. When using data type char to print a character, we should only use '' not "".
2. The format specifier %s is used in printing string.
3. The format specifier should be %c.
*/
