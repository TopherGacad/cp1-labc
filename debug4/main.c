#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/*
    REASONING:
    1. A single char should use sing quotes
    2. The format specifier should be %c instead of %s

*/