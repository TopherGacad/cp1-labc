#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
1. it should be 'A' and not "A". single quotaion, not double.
2. the format specifier to be used must be '%c' because it is 'char'.
 the '%s" is for string.



*/
