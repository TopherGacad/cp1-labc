#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
When using the char data type, ' ' are used instead of " ".
Also, the format specifier used should be %c.
*/
