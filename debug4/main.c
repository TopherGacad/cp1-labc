#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c", grade);
    return 0;
}

/**
REASONING: 1. it should use %c format specifier instead of %s\n because grade is a single character
2. the grade should be enclosed in single quotes ' ' instead of double quotes " "


*/
