#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/*
REASONING:
1. The quotation marks used should be single and not double.
2. The format specifier used is wrong and it should be %c,
as the value of grade is only a single letter.

*/
