#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
1. grade should not have double quotation marks if it is just a single character
2. you can use single quotation mark
3. format specifier and data type should match

*/
