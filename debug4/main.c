#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
1. we dont use double qoutes for a single character, it should be a single qoute
2. the format specifier used is for string so it is wrong, it should be %c

*/
