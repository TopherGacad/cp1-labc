#include <stdio.h>

int main() {
    char grade = 'A';
    printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
    1. the value of the grade had double
    quotations which are used for multiple
    characters, since the value is a single
    character, we must also use single
    quotation
    2. the format specifier did not match
    the data type

*/
