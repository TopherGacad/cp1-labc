#include <stdio.h>
int main()
{
   char grade = 'A';
   printf("Grade: %c\n", grade);
    return 0;
}

/**
REASONING:
    1. Character should not use quotation only if it is string of characters
    2. You can use single quotation or apostrophe for character
    3. Format specifier should match type %c

*/
