#include <stdio.h>

int main()
{
    printf("Hello world!\n");
    return 0;
}

/**
REASONING:
     The code works, but the <stdlib.h> header isn’t needed
     because the program doesn’t use any functions from that library.
     or maybe may nagiba po sir?
*/
